{% extends 'base.html.twig' %}

{% block title %}Édition de {{ "#{contact.lastname}, #{contact.firstname}" }}{% endblock %}

{% block body %}
    <h1>Édition de {{ "#{contact.lastname}, #{contact.firstname}" }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type="button" class="btn btn-primary w-100 p-3">Modifier</button>
    {{ form_end(form) }}

{% endblock %}



